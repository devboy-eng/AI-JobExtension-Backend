# Production Environment Configuration
# Copy this to .env and add actual values for deployment

RAILS_ENV=production
DATABASE_URL=postgresql://username:password@host:5432/database_name
REDIS_URL=redis://redis:6379

# Rails Master Key (required for production)
# Generate with: rails credentials:edit
# RAILS_MASTER_KEY=your_master_key_here

# For deployment platforms like Railway, Render, etc.
# Set these as environment variables in your deployment dashboard:
# - RAILS_MASTER_KEY (from config/master.key)
# - DATABASE_URL (provided by platform)
# - REDIS_URL (if using Redis)

# All sensitive data (API keys, passwords, secrets) are now stored in
# Rails encrypted credentials (config/credentials.yml.enc)
# To edit: EDITOR=nano rails credentials:edit